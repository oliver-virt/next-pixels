/**
 * Facebook Pixel script loader component.
 *
 * Add this to your root layout to initialize the pixel.
 * Uses `afterInteractive` strategy for optimal loading.
 *
 * @example
 * ```tsx
 * // app/layout.tsx
 * import { FacebookPixel } from "next-pixels";
 *
 * export default function RootLayout({ children }) {
 *   return (
 *     <html><body>
 *       {children}
 *       <FacebookPixel />
 *     </body></html>
 *   );
 * }
 * ```
 */
export declare function FacebookPixel(): import("react/jsx-runtime").JSX.Element | null;
//# sourceMappingURL=FacebookPixel.d.ts.map