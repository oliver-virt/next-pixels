# Dummy IDs — the demo works with these in dev mode (server returns mocks).
# Replace with real values to send live events.
NEXT_PUBLIC_FB_PIXEL_ID=1234567890
NEXT_PUBLIC_TIKTOK_PIXEL_ID=TESTSDKID123
NEXT_PUBLIC_GOOGLE_ADS_ID=AW-123456789
NEXT_PUBLIC_GOOGLE_ADS_LABEL=DEFAULTLBL

# Server-only secrets (needed only for real, non-dev delivery):
# FB_PIXEL_ACCESS_TOKEN=
# TIKTOK_ACCESS_TOKEN=
